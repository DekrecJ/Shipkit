# {{PROJECT_NAME}} — Delivery Tasks

> ShipKit will decompose requirements into ordered, testable implementation slices.

Each task should include an ID, goal, dependencies, expected files/components, acceptance criteria, and verification command/evidence.
