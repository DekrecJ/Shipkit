# ShipKit
Read `.shipkit/project.json`, `.shipkit/state.json`, and the relevant `.shipkit/*.md` files before substantial project work. Follow ShipKit phases and run deterministic checks before release.
