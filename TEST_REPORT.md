# ShipKit v0.2.0 Test Report

Run `python -m unittest discover -s tests -v`. The v0.2.0 refactor suite validates atomic state recovery, migration, phase guards, scoring, skills, installer, CLI, scaffolding and release checks.
