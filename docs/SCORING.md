# Scoring v1

`specs/scoring-v1.json` is the source of scoring weights and thresholds. `READY` requires the ready threshold and zero blockers. `REVIEW` is available above the review threshold with zero blockers. Otherwise the project is `NOT_READY`. Project-type profiles may override weights/thresholds.
