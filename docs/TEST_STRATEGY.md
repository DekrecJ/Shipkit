# Test Strategy

Tests cover initialization, atomic-write backup behavior, migration, phase guards, force/debt behavior, scoring, skill manifests, project scaffolding, CLI commands, installer layout and release checks.
