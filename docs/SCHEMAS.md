# Schemas

ShipKit v0.2.0 uses `shipkit:project-v1` and `shipkit:state-v1`. Software version and schema version are separate. Legacy v0.1 metadata is migrated on load and can be normalized explicitly with `shipkit migrate`.
