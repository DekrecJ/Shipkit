# Skill Protocol v1

Every skill contains `SKILL.md` plus `manifest.json`. The manifest versions the protocol and declares expected reads, state mutation policy and structured output fields. Blueprints/capabilities are not duplicated in skill manifests.
