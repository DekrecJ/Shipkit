# Codex Integration

The installer copies ShipKit skills to `~/.agents/skills/shipkit-*` and adds a managed bootstrap block to `~/.codex/AGENTS.md`. New Codex sessions discover those persistent instructions. Project state is not stored in chat memory; it lives in `.shipkit/`.
