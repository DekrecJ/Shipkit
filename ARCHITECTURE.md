# Architecture

ShipKit separates three responsibilities: **Blueprints compose capabilities**, **Capabilities define reusable requirements**, and **Skills define agent procedures**. The Python CLI owns persistent state and verification. Codex/Claude remain the reasoning and implementation engines.
